import { WordActivityCommon } from './word-activity-common';

describe('WordActivityCommon', () => {
  it('should create an instance', () => {
    expect(new WordActivityCommon()).toBeTruthy();
  });
});
